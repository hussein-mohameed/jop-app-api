/**
 * @file Main theme configuration that aggregates all design tokens.
 * Import this single file to access the complete design system.
 */

export { colors, semanticColors } from './colors';
export { fontFamilies, fontSizes, fontWeights, lineHeights, letterSpacings } from './typography';
export { spacing, borderRadius } from './spacing';
export { shadows, darkShadows } from './shadows';
export { transitions, durations, easings, keyframes } from './animations';
export { breakpoints, containerWidths, sidebarWidths } from './breakpoints';
